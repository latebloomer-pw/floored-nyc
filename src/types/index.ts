export interface Post {
    id: string
    user_id: string
    title: string
    description: string
    location: string
    encounter_date: string
    contact_method: string
    created_at: string
    updated_at: string
}